_common: !include ../../package.common.yaml
<<: *common
<<: *flags

name: example-app
version: 0.1.0.0

dependencies:
  - base

executables:
  example-app:
    source-dirs: app
    main: Main.hs
    dependencies:
      - example-lib
